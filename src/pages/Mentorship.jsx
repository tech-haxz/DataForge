import { ArrowRight, Calendar, CheckCircle2, MessageCircle, ShieldCheck } from 'lucide-react'
import { useState } from 'react'
import Reveal from '../components/Reveal'
const mentors = ['Product & strategy', 'Frontend engineering', 'Data & AI', 'Career & portfolios']
export default function Mentorship() { const [sent,setSent] = useState(false); return <div className="container pb-24 pt-36"><div className="grid gap-14 lg:grid-cols-[1fr_.85fr] lg:items-start"><Reveal><div className="eyebrow"><span className="dot"/> 1:1, when it matters</div><h1 className="display mt-6">You don’t have to<br/><span className="text-lime">figure it out alone.</span></h1><p className="mt-7 max-w-lg text-lg leading-relaxed text-muted">A practical, human conversation with someone a few steps ahead — focused on your work, your questions, and your next move.</p><div className="mt-10 space-y-4">{['Get unstuck on a technical problem', 'Make your portfolio impossible to ignore', 'Turn “I’m interested in…” into a plan'].map(item=><div className="flex items-center gap-3 text-sm" key={item}><CheckCircle2 size={18} className="text-lime"/>{item}</div>)}</div></Reveal><Reveal delay={.15}><div className="card"><div className="flex items-center gap-3"><div className="icon-box"><Calendar size={20}/></div><div><h2 className="font-semibold">Book a conversation</h2><p className="text-sm text-muted">20 minutes · completely free</p></div></div>{sent ? <div className="mt-10 rounded-xl border border-lime/30 bg-lime/10 p-6 text-center"><CheckCircle2 className="mx-auto text-lime" size={30}/><h3 className="mt-4 text-lg font-semibold">You’re on the list.</h3><p className="mt-2 text-sm text-muted">We’ll reach out with a time that works.</p></div> : <form className="mt-8 space-y-4" onSubmit={e => {e.preventDefault(); setSent(true)}}><label className="field-label">What would you like to talk about?<select className="field" defaultValue=""><option value="" disabled>Select a focus</option>{mentors.map(x=><option key={x}>{x}</option>)}</select></label><label className="field-label">Your email<input className="field" type="email" placeholder="you@example.com" required/></label><label className="field-label">Anything we should know?<textarea className="field min-h-24 resize-none" placeholder="A little context helps..."/></label><button className="btn-primary w-full justify-center">Find my mentor <ArrowRight size={16}/></button></form>}<div className="mt-7 flex items-center justify-center gap-2 text-xs text-muted"><ShieldCheck size={14} className="text-lime"/> No spam. No sales pitch. Just a useful conversation.</div></div></Reveal></div><section className="mt-24 grid gap-5 border-t border-line pt-20 md:grid-cols-3">{[[MessageCircle,'Ask better questions','Bring the messy version. That’s where the useful answers live.'],[ShieldCheck,'Honest feedback','Specific, kind, and actionable — every time.'],[Calendar,'Keep the momentum','Leave with one clear next step you can actually take.']].map(([Icon,title,text],i)=><Reveal delay={i*.08} key={title}><div className="card"><Icon className="text-lime" size={22}/><h3 className="mt-5 font-semibold">{title}</h3><p className="mt-2 text-sm leading-relaxed text-muted">{text}</p></div></Reveal>)}</section></div> }
