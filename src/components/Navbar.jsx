import { Link, NavLink } from 'react-router-dom'
import { Menu, X, ArrowUpRight } from 'lucide-react'
import ThemeToggle from './ThemeToggle'
const links = [['What we do', '/'], ['Projects', '/projects'], ['Courses', '/courses'], ['Mentorship', '/mentorship']]
export default function Navbar({ menuOpen, setMenuOpen }) { return <header className="fixed inset-x-0 top-0 z-50 border-b border-line/70 bg-canvas/80 backdrop-blur-xl"><div className="container flex h-[72px] items-center justify-between"><Link to="/" className="text-lg font-extrabold tracking-tight" onClick={() => setMenuOpen(false)}>coding<span className="text-lime">mindset</span><span className="ml-1 text-lime">.</span></Link><nav className="hidden items-center gap-7 text-sm font-medium md:flex">{links.map(([label, to]) => <NavLink key={label} to={to} className={({isActive}) => `transition-colors ${isActive ? 'text-ink' : 'text-muted hover:text-ink'}`}>{label}</NavLink>)} </nav><div className="hidden items-center gap-3 md:flex"><ThemeToggle /><Link to="/mentorship" className="btn-primary px-4 py-2 text-sm">Join the next cohort <ArrowUpRight size={15}/></Link></div><button className="md:hidden" onClick={() => setMenuOpen(!menuOpen)} aria-label="Toggle menu">{menuOpen ? <X/> : <Menu/>}</button></div>{menuOpen && <div className="container border-t border-line py-5 md:hidden"><nav className="flex flex-col gap-5 text-sm">{links.map(([label,to]) => <NavLink key={label} to={to} onClick={() => setMenuOpen(false)} className="text-muted">{label}</NavLink>)}<div className="flex items-center justify-between pt-2"><ThemeToggle/><Link to="/mentorship" className="btn-primary px-4 py-2 text-sm" onClick={() => setMenuOpen(false)}>Join the next cohort <ArrowUpRight size={15}/></Link></div></nav></div>}</header> }
